{% extends layout('users') %}

{% block content %}

    <div class="container">

        <div class="jumbotron text-center">
            <p>{{ user.id }}</p>
            <p>{{ user.username }}</p>
            <p>{{ user.email }}</p>
            <p>Joined {{ user.created_at }}</p>
        </div>

    </div>

{% endblock %}